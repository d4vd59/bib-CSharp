<Window x:Class="Quizduell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quizduell"
        mc:Ignorable="d"
        Title="Quiz Verwaltung" Height="650" Width="1000">
    <TabControl Margin="10">

        <TabItem Header="Kategorien">
            <StackPanel Margin="20">
                <TextBlock Text="Kategorien verwalten" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="CategoryNameTextBox" 
                             Width="300" 
                             Margin="0,0,10,0"
                             Padding="5"/>
                    <Button Content="Kategorie hinzufügen" 
                            Click="AddCategory_Click" 
                            Width="150"
                            Margin="0,0,5,0"/>
                    <Button Content="Bearbeiten" 
                            Click="EditCategory_Click" 
                            Width="100"
                            Margin="0,0,5,0"/>
                    <Button Content="Löschen" 
                            Click="DeleteCategory_Click"
                            Width="100"/>
                </StackPanel>
                
                <Button Content="Kategorien laden" 
                        Click="LoadCategories_Click" 
                        Width="150"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,10"/>
                
                <DataGrid x:Name="CategoriesGrid" 
                          Height="250"/>
            </StackPanel>
        </TabItem>
        

        <TabItem Header="Fragen">
            <StackPanel Margin="20">
                <TextBlock Text="Fragen verwalten" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="text:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="QuestionTextTextBox" Grid.Row="0" Grid.Column="1" Width="500" HorizontalAlignment="Left" Padding="5" Margin="0,0,0,5"/>
                    
                    <TextBlock Text="category:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="QuestionCategoryIdTextBox" Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left" Padding="5" Margin="0,0,0,5"/>
                    
                    <TextBlock Text="media:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
                        <Button x:Name="SelectMediaButton" 
                                Content="Datei auswählen..." 
                                Click="SelectMedia_Click"
                                Width="150"
                                Margin="0,0,10,0"/>
                        <TextBlock x:Name="SelectedMediaLabel" 
                                   Text="Keine Datei ausgewählt" 
                                   VerticalAlignment="Center"
                                   Foreground="Gray"/>
                    </StackPanel>
                    
                    <TextBlock Text="difficulty:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="QuestionDifficultyTextBox" Grid.Row="4" Grid.Column="1" Width="100" HorizontalAlignment="Left" Padding="5" Margin="0,0,0,5"/>
                </Grid>
                
                <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                    <Button x:Name="AddQuestionButton" 
                            Content="Frage hinzufügen" 
                            Click="AddQuestion_Click" 
                            Width="150"
                            Margin="0,0,5,0"/>
                    <Button Content="Bearbeiten" 
                            Click="EditQuestion_Click" 
                            Width="100"
                            Margin="0,0,5,0"/>
                    <Button Content="Löschen" 
                            Click="DeleteQuestion_Click"
                            Width="100"/>
                </StackPanel>
                
                <Button Content="Fragen laden" 
                        Click="LoadQuestions_Click" 
                        Width="150"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,10"/>
                
                <DataGrid x:Name="QuestionsGrid" 
                          Height="200" 
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding id}" IsReadOnly="True" Width="50"/>
                        <DataGridTextColumn Header="text" Binding="{Binding text}" Width="*"/>
                        <DataGridTextColumn Header="media" Binding="{Binding media}" Width="120"/>
                        <DataGridTextColumn Header="mime_type" Binding="{Binding mime_type}" Width="100"/>
                        <DataGridTextColumn Header="category" Binding="{Binding category}" Width="80"/>
                        <DataGridTextColumn Header="difficulty" Binding="{Binding difficulty}" Width="80"/>
                        <DataGridTextColumn Header="answered_correctly" Binding="{Binding answered_correctly}" Width="140"/>
                        <DataGridTextColumn Header="answered_incorrectly" Binding="{Binding answered_incorrectly}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </TabItem>
        
        <TabItem Header="Antworten">
            <StackPanel Margin="20">
                <TextBlock Text="Antworten verwalten" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="question:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="QuestionIdTextBox" Grid.Row="0" Grid.Column="1" Width="200" HorizontalAlignment="Left" Padding="5" Margin="0,0,0,5"/>
                    
                    <TextBlock Text="text:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <TextBox x:Name="AnswerTextBox" Grid.Row="1" Grid.Column="1" Width="400" HorizontalAlignment="Left" Padding="5" Margin="0,0,0,5"/>
                    
                    <CheckBox x:Name="CorrectAnswerCheckBox" 
                              Content="correct_answer" 
                              Grid.Row="2" Grid.Column="1"
                              HorizontalAlignment="Left"
                              Margin="0,5,0,0"/>
                </Grid>
                
                <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                    <Button x:Name="AddAnswerButton" Content="Antwort hinzufügen" 
                            Click="AddAnswer_Click" 
                            Width="150"
                            Margin="0,0,5,0"/>
                    <Button Content="Bearbeiten" 
                            Click="EditAnswer_Click" 
                            Width="100"
                            Margin="0,0,5,0"/>
                    <Button Content="Löschen" 
                            Click="DeleteAnswer_Click"
                            Width="100"/>
                </StackPanel>
                
                <Button Content="Antworten laden" 
                        Click="LoadAnswers_Click" 
                        Width="150"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,10"/>
                
                <DataGrid x:Name="AnswersGrid" 
                          Height="200" 
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding id}" IsReadOnly="True" Width="50"/>
                        <DataGridTextColumn Header="question" Binding="{Binding question}" Width="100"/>
                        <DataGridTextColumn Header="text" Binding="{Binding text}" Width="*"/>
                        <DataGridCheckBoxColumn Header="correct_answer" Binding="{Binding correct_answer}" Width="120"/>
                        <DataGridTextColumn Header="answered_correctly" Binding="{Binding answered_correctly}" Width="140"/>
                        <DataGridTextColumn Header="answered_incorrectly" Binding="{Binding answered_incorrectly}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
